{% schema %}
{
  "name": "Carrossel Coverflow",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "titulo",
      "label": "Título",
      "default": "Destaques"
    },
    {
      "type": "select",
      "id": "tamanho_card",
      "label": "Tamanho dos cards",
      "default": "medio",
      "options": [
        { "value": "pequeno", "label": "Pequeno" },
        { "value": "medio", "label": "Médio" },
        { "value": "grande", "label": "Grande" }
      ]
    },
    {
      "type": "range",
      "id": "stretch",
      "label": "Stretch (espaçamento lateral)",
      "min": 0,
      "max": 200,
      "step": 10,
      "default": 80
    },
    {
      "type": "range",
      "id": "rotate",
      "label": "Rotate (graus de rotação)",
      "min": 0,
      "max": 90,
      "step": 5,
      "default": 0
    },
    {
      "type": "range",
      "id": "depth",
      "label": "Depth (profundidade 3D)",
      "min": 0,
      "max": 1000,
      "step": 50,
      "default": 200
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Ativar autoplay",
      "default": true
    },
    {
      "type": "range",
      "id": "autoplay_delay",
      "label": "Autoplay delay (ms)",
      "min": 1000,
      "max": 9000,
      "step": 500,
      "default": 3000
    }
  ],
  "blocks": [
    {
      "type": "product",
      "name": "Produto",
      "settings": [
        {
          "type": "product",
          "id": "produto",
          "label": "Selecionar produto"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Carrossel Coverflow",
      "category": "Custom"
    }
  ]
}
{% endschema %}

<div class="coverflow-section page-width">
  {% if section.settings.titulo != blank %}
    <h2 class="section-title">{{ section.settings.titulo }}</h2>
  {% endif %}

  <div class="swiper">
    <div class="swiper-wrapper">
      {% for block in section.blocks %}
        {% assign produto = block.settings.produto %}
        {% if produto != blank %}
          <div class="swiper-slide">
            <a href="{{ produto.url }}">
              <img 
                src="{{ produto.featured_image | image_url: width: 600 }}" 
                alt="{{ produto.title }}" 
                class="card-img">
            </a>
            <p class="product-title">{{ produto.title }}</p>
          </div>
        {% endif %}
      {% endfor %}
    </div>

    <div class="swiper-button-next"></div>
    <div class="swiper-button-prev"></div>
  </div>
</div>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css">
<style>
  .coverflow-section {
    margin: 40px 0 60px;
    text-align: center;
  }

  .swiper {
    width: 70% !important;
    max-width: 1400px !important;
    margin: auto;
  }

  .swiper-slide {
    background: rgba(255, 255, 255, 0.1);
    border: 2px solid rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    border-radius: 20px;
    padding: 40px 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    transition: all 0.4s ease;
  }

  .swiper-slide:hover {
    filter: contrast(1.07) saturate(1.1);
  }

  .swiper-slide-prev,
  .swiper-slide-next {
    opacity: 0.6;
  }

  .swiper-slide:not(.swiper-slide-active):not(.swiper-slide-prev):not(.swiper-slide-next) {
    opacity: 0.2;
  }

  .card-img {
    border-radius: 15px !important;
    margin-bottom: 15px !important;
    transition: transform 0.4s ease !important;
  }

  .card-img:hover {
    transform: scale(1.1) translateY(-10px) !important;
  }

  .product-title {
    color: white;
    margin-top: 10px !important;
    font-size: 1.1rem !important;
  }

  .swiper-button-next,
  .swiper-button-prev {
    color: white !important;
  }

  /* Tamanhos dos cards */
  {% if section.settings.tamanho_card == 'pequeno' %}
    .card-img { max-width: 250px !important; width: 100% !important; }
  {% elsif section.settings.tamanho_card == 'medio' %}
    .card-img { max-width: 350px !important; width: 100% !important; }
  {% elsif section.settings.tamanho_card == 'grande' %}
    .card-img { max-width: 500px !important; width: 100% !important; }
  {% endif %}
</style>

<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
<script>
  document.addEventListener('DOMContentLoaded', function() {
    const swiper = new Swiper('.coverflow-section .swiper', {
      effect: 'coverflow',
      grabCursor: true,
      centeredSlides: true,
      slidesPerView: 'auto',
      loop: true,

      coverflowEffect: {
        rotate: {{ section.settings.rotate }},
        stretch: {{ section.settings.stretch }},
        depth: {{ section.settings.depth }},
        modifier: 1,
        slideShadows: false,
      },

      navigation: {
        nextEl: '.coverflow-section .swiper-button-next',
        prevEl: '.coverflow-section .swiper-button-prev',
      },

      keyboard: { enabled: true },

      {% if section.settings.autoplay %}
      autoplay: {
        delay: {{ section.settings.autoplay_delay }},
        disableOnInteraction: false,
      },
      {% endif %}
    });
  });
</script>